module.exports = function(needle, mysql) {

  //  Some globals

let 

 

  return {
    sleep: sleep,
    reconnect: reconnect,
    getAllRules: getAllRules,
    deleteAllRules: deleteAllRules,
    setRules: setRules,
    sqlWatcher: sqlWatcher,
    sendOnDbUpdate: sendOnDbUpdate,
    checkup: checkup,
    // tweetOverflow: tweetOverflow,
    areThereCats: areThereCats,
  };
};
